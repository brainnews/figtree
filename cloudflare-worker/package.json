{
  "name": "treekit-oauth-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Treekit OAuth token exchange",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}